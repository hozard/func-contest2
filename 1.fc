{- Метод, подсчитывающий наибольший общий делитель двух целых чисел от 1 до 1048576. -}
() recv_internal() {
}

{- Функция, вычисляющая наибольший общий делитель двух целых чисел. -}
(int) gcd(int a, int b) method_id {
    int temp;
    while (b != 0) {
        temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}
